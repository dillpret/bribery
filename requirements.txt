# Production dependencies
Flask==2.3.3
Flask-SocketIO==5.3.6
python-socketio==5.8.0
python-engineio==4.7.1
eventlet==0.35.2
gevent==23.9.1
requests==2.32.4
websocket-client==1.8.0
gunicorn==21.2.0

# Network dependencies (requires C compilation)
netifaces==0.11.0  # Explicitly add this to control the version

# Additional production dependencies
psutil==5.9.5
dnspython==2.4.2

# Testing dependencies
pytest==8.4.1
# socketio 0.2.1 has a dependency on setuptools==3.3 which conflicts with newer versions
# Moving this to requirements-test.txt as it's only needed for tests, not production
# socketio==0.2.1

# Development dependencies (code quality)
# Install with: py -m pip install -r requirements-dev.txt
# Or individually: py -m pip install flake8 black isort mypy bandit pre-commit
